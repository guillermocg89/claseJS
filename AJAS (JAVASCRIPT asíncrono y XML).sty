AJAS (JAVASCRIPT asíncrono y XML)

CLIENTE: Navegador
SERVIDOR: hosting o alojamiento

cuando un usuario se conecta a una pagina lo primero que se carga es el
HTML, con junto con sus animaciones (css) y alunos eventos hechos en JS.

de igual forma existen tecnologias que nos permitencomunicarnos con
la base de datos. (PHP)

un formulario en HTML envia datos hacia una pagina PHP , por ejemplo,
usando el envio deparámetros por POST o por GUET (que son peticiones http)